<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Portfolio Photographie</title>
    <!-- Polices Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;600&family=Playfair+Display:ital,wght@0,400;0,600;1,400&display=swap" rel="stylesheet">
    
    <style>
        /* --- RESET & VARIABLES --- */
        :root {
            --bg-color: #0d0d0d;
            --text-color: #f5f5f5;
            --text-muted: #a0a0a0;
            --accent-color: #ffffff;
            --card-bg: #1a1a1a;
            --transition-speed: 0.3s;
            --font-title: 'Playfair Display', serif;
            --font-body: 'Montserrat', sans-serif;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html {
            scroll-behavior: smooth;
        }

        body {
            background-color: var(--bg-color);
            color: var(--text-color);
            font-family: var(--font-body);
            line-height: 1.6;
            overflow-x: hidden;
        }

        /* --- EN-TÊTE / NAVIGATION --- */
        header {
            position: fixed;
            top: 0;
            width: 100%;
            padding: 2rem 5%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            z-index: 1000;
            background: linear-gradient(to bottom, rgba(13,13,13,0.9), rgba(13,13,13,0));
            backdrop-filter: blur(5px);
        }

        .logo {
            font-family: var(--font-title);
            font-size: 1.5rem;
            font-weight: 600;
            letter-spacing: 2px;
            text-transform: uppercase;
        }

        nav a {
            color: var(--text-color);
            text-decoration: none;
            margin-left: 2rem;
            font-size: 0.9rem;
            text-transform: uppercase;
            letter-spacing: 1px;
            transition: color var(--transition-speed);
        }

        nav a:hover {
            color: var(--text-muted);
        }

        /* --- HERO SECTION --- */
        .hero {
            height: 70vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            padding: 0 1rem;
        }

        .hero h1 {
            font-family: var(--font-title);
            font-size: clamp(2.5rem, 6vw, 4.5rem);
            font-weight: 400;
            margin-bottom: 1rem;
            letter-spacing: 1px;
        }

        .hero p {
            color: var(--text-muted);
            font-size: clamp(1rem, 2vw, 1.2rem);
            max-width: 600px;
            font-weight: 300;
        }

        /* --- GALERIE RESPONSIVE --- */
        .gallery-container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 2rem 5% 6rem 5%;
        }

        .gallery-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
            gap: 2rem;
        }

        .gallery-item {
            position: relative;
            overflow: hidden;
            border-radius: 4px;
            cursor: pointer;
            aspect-ratio: 3 / 4;
            background-color: var(--card-bg);
        }

        .gallery-item img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.6s cubic-bezier(0.25, 1, 0.5, 1);
            filter: grayscale(20%);
        }

        .gallery-item:hover img {
            transform: scale(1.05);
            filter: grayscale(0%);
        }

        .gallery-overlay {
            position: absolute;
            inset: 0;
            background: rgba(0, 0, 0, 0.4);
            opacity: 0;
            display: flex;
            flex-direction: column;
            justify-content: flex-end;
            padding: 1.5rem;
            transition: opacity var(--transition-speed) ease;
        }

        .gallery-item:hover .gallery-overlay {
            opacity: 1;
        }

        .gallery-overlay h3 {
            font-family: var(--font-title);
            font-size: 1.25rem;
            font-weight: 400;
        }

        .gallery-overlay p {
            font-size: 0.85rem;
            color: var(--text-muted);
        }

        /* --- LIGHTBOX (ZOOM PHOTO) --- */
        .lightbox {
            position: fixed;
            inset: 0;
            background-color: rgba(13, 13, 13, 0.95);
            z-index: 2000;
            display: flex;
            justify-content: center;
            align-items: center;
            opacity: 0;
            pointer-events: none;
            transition: opacity var(--transition-speed) ease;
        }

        .lightbox.active {
            opacity: 1;
            pointer-events: auto;
        }

        .lightbox-img {
            max-width: 90%;
            max-height: 85vh;
            object-fit: contain;
            box-shadow: 0 10px 30px rgba(0,0,0,0.5);
            transform: scale(0.95);
            transition: transform var(--transition-speed) ease;
        }

        .lightbox.active .lightbox-img {
            transform: scale(1);
        }

        .lightbox-close {
            position: absolute;
            top: 2rem;
            right: 2rem;
            color: var(--text-color);
            font-size: 2.5rem;
            cursor: pointer;
            background: none;
            border: none;
            line-height: 1;
            transition: color var(--transition-speed);
        }

        .lightbox-close:hover {
            color: var(--text-muted);
        }

        .lightbox-caption {
            position: absolute;
            bottom: 2rem;
            color: var(--text-muted);
            font-size: 0.9rem;
            letter-spacing: 1px;
            text-align: center;
        }

        /* --- SECTION CONTACT --- */
        .contact-section {
            max-width: 700px;
            margin: 0 auto;
            padding: 4rem 5% 8rem 5%;
            text-align: center;
        }

        .contact-section h2 {
            font-family: var(--font-title);
            font-size: 2.5rem;
            margin-bottom: 1rem;
            font-weight: 400;
        }

        .contact-section p {
            color: var(--text-muted);
            margin-bottom: 3rem;
        }

        .contact-form {
            display: flex;
            flex-direction: column;
            gap: 1.5rem;
        }

        .form-group {
            display: flex;
            flex-direction: column;
            text-align: left;
        }

        .form-group input,
        .form-group textarea {
            background-color: transparent;
            border: none;
            border-bottom: 1px solid rgba(255, 255, 255, 0.2);
            padding: 1rem 0;
            color: var(--text-color);
            font-family: var(--font-body);
            font-size: 1rem;
            outline: none;
            transition: border-color var(--transition-speed);
        }

        .form-group input:focus,
        .form-group textarea:focus {
            border-bottom-color: var(--accent-color);
        }

        .btn-submit {
            margin-top: 1rem;
            padding: 1rem 2rem;
            background-color: var(--accent-color);
            color: var(--bg-color);
            border: none;
            font-family: var(--font-body);
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 2px;
            cursor: pointer;
            transition: background-color var(--transition-speed), transform var(--transition-speed);
            align-self: center;
        }

        .btn-submit:hover {
            background-color: #e0e0e0;
            transform: translateY(-2px);
        }

        /* --- FOOTER --- */
        footer {
            padding: 2rem;
            text-align: center;
            border-top: 1px solid rgba(255, 255, 255, 0.05);
            color: var(--text-muted);
            font-size: 0.8rem;
        }
    </style>
</head>
<body>

    <!-- EN-TÊTE -->
    <header>
        <div class="logo">Alexandre V.</div>
        <nav>
            <a href="#galerie">Galerie</a>
            <a href="#contact">Contact</a>
        </nav>
    </header>

    <!-- ACCUEIL / HERO -->
    <section class="hero">
        <h1>Capturer l'Essentiel</h1>
        <p>Photographie d'architecture, de paysage et de portraits minimalistes.</p>
    </section>

    <!-- GALERIE -->
    <main class="gallery-container" id="galerie">
        <div class="gallery-grid">
            
            <!-- Photo 1 -->
            <div class="gallery-item" data-full="https://images.unsplash.com/photo-1506744038136-46273834b3fb?auto=format&fit=crop&w=1600&q=80" data-caption="Vallée brumeuse — Alpes">
                <img src="https://images.unsplash.com/photo-1506744038136-46273834b3fb?auto=format&fit=crop&w=800&q=80" alt="Paysage montagne">
                <div class="gallery-overlay">
                    <h3>Vallée brumeuse</h3>
                    <p>Alpes, 2023</p>
                </div>
            </div>

            <!-- Photo 2 -->
            <div class="gallery-item" data-full="https://images.unsplash.com/photo-1513694203232-719a280e022f?auto=format&fit=crop&w=1600&q=80" data-caption="Lignes & Ombres — Architecture">
                <img src="https://images.unsplash.com/photo-1513694203232-719a280e022f?auto=format&fit=crop&w=800&q=80" alt="Architecture minimaliste">
                <div class="gallery-overlay">
                    <h3>Lignes & Ombres</h3>
                    <p>Design d'intérieur</p>
                </div>
            </div>

            <!-- Photo 3 -->
            <div class="gallery-item" data-full="https://images.unsplash.com/photo-1469474968028-56623f02e42e?auto=format&fit=crop&w=1600&q=80" data-caption="Solitude Minérale — Écosse">
                <img src="https://images.unsplash.com/photo-1469474968028-56623f02e42e?auto=format&fit=crop&w=800&q=80" alt="Nature sauvage">
                <div class="gallery-overlay">
                    <h3>Solitude Minérale</h3>
                    <p>Écosse, 2022</p>
                </div>
            </div>

            <!-- Photo 4 -->
            <div class="gallery-item" data-full="https://images.unsplash.com/photo-1534528741775-53994a69daeb?auto=format&fit=crop&w=1600&q=80" data-caption="Regard Extérieur — Portrait">
                <img src="https://images.unsplash.com/photo-1534528741775-53994a69daeb?auto=format&fit=crop&w=800&q=80" alt="Portrait">
                <div class="gallery-overlay">
                    <h3>Regard Extérieur</h3>
                    <p>Série Portraits</p>
                </div>
            </div>

            <!-- Photo 5 -->
            <div class="gallery-item" data-full="https://images.unsplash.com/photo-1470071459604-3b5ec3a7fe05?auto=format&fit=crop&w=1600&q=80" data-caption="Brumes du Matin — Forêt Noir">
                <img src="https://images.unsplash.com/photo-1470071459604-3b5ec3a7fe05?auto=format&fit=crop&w=800&q=80" alt="Forêt dans la brume">
                <div class="gallery-overlay">
                    <h3>Brumes du Matin</h3>
                    <p>Forêt Noire, 2023</p>
                </div>
            </div>

            <!-- Photo 6 -->
            <div class="gallery-item" data-full="https://images.unsplash.com/photo-1486406146926-c627a92ad1ab?auto=format&fit=crop&w=1600&q=80" data-caption="Reflets Urbains — Tokyo">
                <img src="https://images.unsplash.com/photo-1486406146926-c627a92ad1ab?auto=format&fit=crop&w=800&q=80" alt="Grate-ciel moderne">
                <div class="gallery-overlay">
                    <h3>Reflets Urbains</h3>
                    <p>Tokyo, 2024</p>
                </div>
            </div>

        </div>
    </main>

    <!-- LIGHTBOX (ZOOM) -->
    <div class="lightbox" id="lightbox">
        <button class="lightbox-close" id="lightbox-close">&times;</button>
        <img src="" alt="Photo agrandie" class="lightbox-img" id="lightbox-img">
        <div class="lightbox-caption" id="lightbox-caption"></div>
    </div>

    <!-- SECTION CONTACT -->
    <section class="contact-section" id="contact">
        <h2>Travaillons Ensemble</h2>
        <p>Une idée de projet, une exposition ou une question ? Envoyez-moi un message.</p>
        
        <form class="contact-form" onsubmit="event.preventDefault(); alert('Message envoyé !');">
            <div class="form-group">
                <input type="text" placeholder="Votre Nom" required>
            </div>
            <div class="form-group">
                <input type="email" placeholder="Votre Adresse Email" required>
            </div>
            <div class="form-group">
                <textarea rows="4" placeholder="Votre Message" required></textarea>
            </div>
            <button type="submit" class="btn-submit">Envoyer</button>
        </form>
    </section>

    <!-- FOOTER -->
    <footer>
        <p>&copy; 2024 Alexandre V. Tous droits réservés.</p>
    </footer>

    <!-- JAVASCRIPT -->
    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const galleryItems = document.querySelectorAll('.gallery-item');
            const lightbox = document.getElementById('lightbox');
            const lightboxImg = document.getElementById('lightbox-img');
            const lightboxCaption = document.getElementById('lightbox-caption');
            const lightboxClose = document.getElementById('lightbox-close');

            // Ouvrir la Lightbox au clic sur une image
            galleryItems.forEach(item => {
                item.addEventListener('click', () => {
                    const fullImgUrl = item.getAttribute('data-full');
                    const captionText = item.getAttribute('data-caption');

                    lightboxImg.src = fullImgUrl;
                    lightboxCaption.textContent = captionText;
                    lightbox.classList.add('active');
                    document.body.style.overflow = 'hidden'; // Empêche le défilement sous la lightbox
                });
            });

            // Fermer la Lightbox au clic sur le bouton de fermeture
            const closeLightbox = () => {
                lightbox.classList.remove('active');
                document.body.style.overflow = 'auto'; // Rétablit le défilement
                setTimeout(() => {
                    lightboxImg.src = ''; // Libère la mémoire
                }, 300);
            };

            lightboxClose.addEventListener('click', closeLightbox);

            // Fermer la Lightbox au clic à l'extérieur de l'image
            lightbox.addEventListener('click', (e) => {
                if (e.target === lightbox) {
                    closeLightbox();
                }
            });

            // Fermer la Lightbox avec la touche Échap (Escape)
            document.addEventListener('keydown', (e) => {
                if (e.key === 'Escape' && lightbox.classList.contains('active')) {
                    closeLightbox();
                }
            });
        });
    </script>
</body>
</html>